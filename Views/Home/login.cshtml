@*  Documentation Header
    Author: Mikayle Coetzee (ST10023767)
    Course: PROG7311 POE 2023
    Part: 2                              *@

@{
    ViewData["Title"] = "Login Page";
    Layout = null;
}

@model ST10023767_PROG.Models.RegisterViewModel

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/ST10023767_POE.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="path/to/toastr.min.css">
    <script src="path/to/toastr.min.js"></script>

    <style type="text/css">
        html, body {
            background-image: url(/Images/signup_image.jpg);
            height: 100%;
            margin: 0;
            background-repeat: no-repeat;
            background-size: cover;
            color: white;
            position: relative;
        }

        .content {
            text-align: center;
            max-width: 500px;
            width: 400px;
            padding: 20px;
            background-color: rgba(0, 0, 0, 0.5);
            border-radius: 10px;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        .footer {
            padding: 10px;
            background-color: #343a40;
            color: white;
            text-align: center;
        }
    </style>
</head>
<body>
    <header>
        <nav class="sb-topnav navbar navbar-expand navbar-dark bg-dark">
            <!-- Navbar Brand-->
            <a class="navbar-brand ps-3" asp-area="" asp-controller="Home" asp-action="Login">Agri-Energy Connect</a>
            <!-- Sidebar Toggle-->
            <button class="btn btn-link btn-sm order-1 order-lg-0 me-4 me-lg-0" id="sidebarToggle" href="#!"><i class="fas fa-bars"></i></button>
            <!-- Navbar-->
            <div class="collapse navbar-collapse" id="navbarColor02">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" asp-area="" asp-controller="Home" asp-action="AboutUs">About Us</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
                    </li>
                </ul>
            </div>
        </nav>
    </header>

    <div class="content">
        <h1 class="display-4">Log In</h1>
        <hr /> <!-- Added line underneath the heading -->
        <form action="/Home/Login" method="post" style="max-width: 400px; margin: 0 auto;">
            <div class="form-group">
                <input asp-for="Username" class="form-control mb-2" placeholder="Username" required autofocus>
                <span asp-validation-for="Username" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input asp-for="Password" type="password" class="form-control mb-2" placeholder="Password" required>
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>

            <div class="form-group d-flex justify-content-between">
                <a asp-area="" asp-controller="Home" asp-action="Register" class="btn btn-secondary flex-fill me-2">Sign Up</a>
                <button type="submit" class="btn btn-primary flex-fill ms-2">Sign In</button>
            </div>

            <label></label>
        </form>
        <button onclick="goBack()" class="btn btn-primary">Back</button>

        <script>
            function goBack() {
                window.history.back();
            }

            
                document.addEventListener("DOMContentLoaded", function () {
                    var errorMessage = '@TempData["ErrorMessage"]';
                    var successMessage = '@TempData["SuccessMessage"]';

                    if (errorMessage) {
                        alert(errorMessage);
                    }

                    if (successMessage) {
                        alert(successMessage);
                    }
                });
        </script>

    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
